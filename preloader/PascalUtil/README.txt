The PascalUtil class was conceived to allow interoperability between C++ hooks
within a Delphi-Pascal application; It implements methods to make ASCII and
UTF16 strings, as well as a method to free them.